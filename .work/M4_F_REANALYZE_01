M4-F offline reanalysis marker.

Revision 2: re-run the preserved s12 checkpoint after explicit terminal shutdown-tail classification.

Expected publication result:
- source Fetch evidence remains immutable;
- exactly two final-window Fetches remain without scheduling decisions;
- both must classify as terminal unreconciled shutdown-tail Fetches;
- interiorMissingDecisionCount must be zero;
- publication validation must report evidenceComplete=true;
- no Official War API traffic is issued by this workflow.
